/*
Variables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!
newID = 1000000000 + oldID;
ageIsValid = Number.isInteger(currentAge);
randomNumber = Math.floor(Math.random() * 20);
randomInteger = Math.round(randomNumber);

// randomUserID, which should convert randomInteger
// into a valid ID number — an integer between 1000000001 and 100000002
randomUserID = randomInteger + 1000000001;
